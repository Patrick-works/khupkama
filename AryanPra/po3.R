library(party)
print(head(readingSkills))
png(file = "decision_tree.png")
plot(output.tree) 
# Save the file. 
dev.off() 
# Plot the tree. 
plot(output.tree)plot(output.tree) 
# Save the file. 
dev.off() 
# Plot the tree. 
plot(output.tree)